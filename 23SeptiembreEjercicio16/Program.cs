// Algortimos de ordenamiento y busqueda. Tiene que mostyrar el vector original y el vector ya ordenado con el metodo de la burbuja
// De un vector pedirdatos, el alcance de ese vector, luego otra, que se reinicie y luego calcule los datos con el metodo de la burbuja.
namespace OrdenamientoBurbuja
{
    class Burbuja
    {
        // Constante para establecer los limites permitidos.
        const int MIN_TAMANIO = 1;
        const int MAX_TAMANIO = 100;
        
        const int MIN_VALOR = 1;
        const int MAX_VALOR = 1000000;

        static void Main(string[] args)
        {
            bool continuar = true;
            while (continuar)
        }
    }
}